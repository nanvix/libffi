[package]
name = "libffi"
version = "3.4.6"
nanvix-version = "0.12.470"

[builds]
[builds.matrix]
platforms = ["hyperlight", "microvm"]
modes = ["multi-process", "single-process", "standalone"]
memory = ["128mb", "256mb"]

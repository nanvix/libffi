[package]
name = "libffi"
version = "3.4.6"
nanvix-version = "0.13.15"

[builds]
[builds.matrix]
platforms = ["hyperlight", "microvm"]
modes = ["multi-process", "single-process", "standalone"]
memory = ["128mb", "256mb"]
